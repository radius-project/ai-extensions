---
"radius": patch
---

Validate generated verify, deploy, and delete workflows as YAML before they can be committed, surfacing malformed upstream templates or quote-bearing environment and application-file values as local generation errors instead of GitHub Actions HTTP 422 failures.
