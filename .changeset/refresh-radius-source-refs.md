---
"@radius-project/canvas": patch
---

Reload the open Radius canvas after `update_source_refs` attaches code references so source links become active immediately without manually reopening the panel.
