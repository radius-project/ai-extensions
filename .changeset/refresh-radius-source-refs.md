---
"radius": patch
---

Reload the open Radius canvas after `update_source_refs` attaches code references so source links become active immediately without manually reopening the panel. The in-card "View source code" link now opens the file directly on click instead of requiring the node's "•••" popup.
