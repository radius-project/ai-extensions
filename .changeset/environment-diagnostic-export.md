---
"radius": patch
---

Add a customer-initiated, local diagnostic download for Azure environment setup. The default export contains only allowlisted operation states, timing, and aggregate recovery outcomes. Customers can optionally review and include bounded repository, branch, environment, and GitHub login values; resource identities, credential-profile names, worktree paths, persisted inputs, logs, raw evidence, and secrets remain excluded.
