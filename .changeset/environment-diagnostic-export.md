---
"radius": patch
---

Add a customer-initiated, local diagnostic download for Azure environment setup. The export contains only allowlisted operation states, timing, and aggregate recovery outcomes; it excludes resource identifiers, persisted inputs, logs, raw evidence, and secrets.
