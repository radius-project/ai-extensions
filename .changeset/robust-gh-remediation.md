---
"radius": patch
---

Make GitHub CLI remediation commands directly runnable when Radius is using Copilot's bundled `gh` binary. Radius now keeps the short `gh` form for system installations and otherwise displays the discovered private path with PowerShell, zsh, or Bash quoting, plus the option to install GitHub CLI system-wide.
