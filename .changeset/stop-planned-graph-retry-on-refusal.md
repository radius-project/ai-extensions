---
"radius": patch
---

Stop modeled, planned, and diff application graphs from retrying after the app-bicep skill reports that the repository cannot be modeled. Their shared, accessible graph error surface now shows the same refusal message returned to chat instead of leaving progress or stale graph context visible.
