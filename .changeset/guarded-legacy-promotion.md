---
"radius": patch
---

**Fixed:** Reject changed source and staged outputs during model promotion without requiring a new command sequence. Legacy modeling retains its compiler and static checks; stricter lifecycle validation remains separate.
