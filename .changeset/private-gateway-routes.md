---
"radius": patch
---

Document automatic Gateway API prerequisite management for `Radius.Compute/routes`, with a private `ClusterIP` managed Gateway by default, explicit environment-wide public exposure, user-managed Gateway validation, and ownership-safe cleanup after the last routes app is deleted.
