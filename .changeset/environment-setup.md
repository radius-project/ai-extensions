---
"radius": minor
---

Create and verify deploy environments in a guided workflow that keeps the selected cloud and GitHub identities visible. Setup validates credentials, tracks progress, resumes after restarts, and offers safe retry, continue, and rollback controls with redacted diagnostics when attention is required.
