---
"radius": patch
---

**Fixed:** Keep single-color resource-type icons legible in the application graph. Icons that a resource type pack draws in `currentColor` now follow the canvas theme in node cards and the legend instead of always painting black, which made them nearly invisible in dark mode. Multi-color icons, remote icons, and built-in glyphs are unchanged.
