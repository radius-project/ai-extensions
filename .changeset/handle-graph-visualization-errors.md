---
"radius": patch
---

Recover application graph visualization failures by rebuilding modeled graphs from `app.bicep` when the panel reloads, preserving successfully loaded empty graphs, surfacing branch comparison errors, and providing a reload action when the client renderer fails.
