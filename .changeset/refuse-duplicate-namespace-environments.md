---
"radius": patch
---

Refuse an environment whose Kubernetes namespace another environment on the same cluster already holds. Creation fails closed when the existing claims cannot be read, and the wizard states the one-environment-per-namespace rule beside the namespace picker.
