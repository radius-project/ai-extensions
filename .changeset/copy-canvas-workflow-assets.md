---
"radius": patch
---

Copy the bundled Repo Radius workflow tree when repairing Canvas discovery so the extension bundle and generated workflow assets always come from the same marketplace plugin build.
