---
"radius": patch
---

**Fixed:** Keep application graphs tied to the selected workspace or committed revision, and ignore uncommitted edits in pull request comparisons. Distinguish empty and missing definitions from unreadable sources, preventing false resource deletions and automatic authoring after failed reads. Report staging cleanup failures instead of silently ignoring them.
