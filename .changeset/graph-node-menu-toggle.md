---
"radius": patch
---

**Fixed:** Clicking a node in an application graph now toggles its details menu, so clicking the same node again dismisses the menu instead of leaving it open. Clicking another node still re-anchors the menu, clicking the empty pane still closes it, and clicks inside the menu keep its controls usable.
