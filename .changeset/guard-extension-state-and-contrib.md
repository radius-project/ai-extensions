---
"radius": patch
---

Fail closed when Repo Radius startup does not establish restorable state, report interrupted command runs as failures, and validate that extension workflows resolve contrib recipe sources exclusively through an immutable Radius defaults catalog.
