---
"radius": minor
---

Keep **Delete Deployment** as the primary action for failed deployments and surface **Stop tracking deployment** only after a teardown workflow fails. The recovery action removes the failed teardown from Radius Canvas and GitHub without authenticating to a cloud provider or claiming that cloud resources were deleted.
