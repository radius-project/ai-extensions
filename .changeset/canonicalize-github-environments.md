---
"radius": patch
---

Resolve and persist GitHub's canonical Environment name before configuring cloud identity, state, workflows, or verification so case-insensitive environment requests cannot create case-sensitive OIDC subject mismatches.
