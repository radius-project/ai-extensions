---
"radius": minor
---

Remove four unused legacy Radius Canvas action/tool pairs in favor of the canonical page-based canvas flows, while retaining source-reference actions and the six actively supported extension tools. This removes the `configure_oidc`, `create_environment`, `render_graph`, and `render_graph_diff` actions and their `radius_`-prefixed tools from the published extension surface.
