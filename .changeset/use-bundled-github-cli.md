---
"radius": patch
---

Keep the GitHub Copilot app's search path when Radius invokes the GitHub CLI, so the app-bundled `gh` stays resolvable even when a command overrides part of the environment to control GitHub credentials.
