---
"@radius-project/canvas": patch
---

Inherit the GitHub Copilot App theme throughout Radius Canvas. Canvas pages, graphs, dialogs, forms, status states, and logs now use host semantic colors so light, dark, and system-default settings render consistently without a separate Radius theme preference.
