---
"radius": patch
---

Run the application-modeling skill's managed Radius version query in a detached process and terminate its complete process tree on timeout, preventing Windows ARM64 sessions from hanging while `rad` invokes Bicep.
