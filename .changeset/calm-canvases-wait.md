---
"radius": patch
---

Keep Radius inactive in unrelated repositories and make application graph diffs conditional on a successful, branch-matched graph result. Radius no longer blocks pull request creation when the graph is unavailable or an external lookup fails.
