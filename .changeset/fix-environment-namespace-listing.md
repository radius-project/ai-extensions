---
"@radius-project/adapter-canvas": patch
---

Report an environment's Kubernetes namespace from `KUBERNETES_NAMESPACE` when listing environments, so editing an environment no longer silently resets its namespace to `default`.
