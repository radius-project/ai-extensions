---
"radius": minor
---

Manage the shared Gateway API prerequisites for `Radius.Compute/routes` deployments. Deploy workflows now validate explicit user-managed Gateways and compatible listeners or reconcile a private HTTP/TLS Radius-owned Gateway by default, while serialized application deletion safely removes only unused, proven-owned infrastructure.
