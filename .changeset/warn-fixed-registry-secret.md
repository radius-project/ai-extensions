---
"radius": patch
---

**Fixed:** Generated app models now warn that the registry credential Secret's `name` value must remain `radius-ghcr-registry-creds` because image pushes depend on that fixed value.
