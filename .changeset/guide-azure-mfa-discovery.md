---
"radius": patch
---

Turn Azure MFA and interaction-required discovery failures into an actionable Azure CLI sign-in callout. The environment flow now keeps raw authentication diagnostics out of the page, scopes login to a valid selected tenant, and clears the guidance after discovery succeeds.
