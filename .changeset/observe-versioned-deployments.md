---
"radius": minor
---

**Added:** Track qualified lifecycle deployments with source-bound operation identities and separate restore, command, state-save, and cleanup outcomes. Missing final evidence remains unconfirmed, and status polling never starts repairs or repeats deployment.

Canonical deployment requires trusted host approval and qualified versioned workflows. Hosts without that approval integration retain legacy deployment instead of claiming canonical support.
