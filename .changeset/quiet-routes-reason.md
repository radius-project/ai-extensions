---
"radius": patch
---

Require the selected deployment profile to declare external-client ingress before application modeling emits `Radius.Compute/routes`. Browser-facing workloads, app-owned proxies, Compose ports, localhost documentation, and component names no longer imply a route.
