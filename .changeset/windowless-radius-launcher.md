---
"radius": patch
---

Launch every managed Radius CLI process through architecture-specific Windows GUI helpers with a headless pseudoconsole and owned process-tree isolation so commands run without flashing a console window.
