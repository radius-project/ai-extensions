---
"radius": patch
---

Name the source commit in rolling edge versions. A published edge build is now stamped `0.1.0-edge-<short-sha>` instead of `0.1.0-edge-<utc-timestamp>`, so the installed version identifies the `main` commit it was built from.
