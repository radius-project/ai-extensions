{
  "name": "radius",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "main": "com.github.copilot/extensions/radius/extension.mjs",
  "description": "Radius canvas extension: application modeling, graph visualization, and deployment for GitHub Copilot.",
  "scripts": {
    "build": "pnpm --filter @radius-project/adapter-canvas run build",
    "test:artifact": "pnpm --filter @radius-project/adapter-canvas run test:integration:artifact"
  },
  "dependencies": {
    "@github/copilot-sdk": "catalog:"
  },
  "devDependencies": {
    "@radius-project/adapter-canvas": "workspace:*"
  }
}
