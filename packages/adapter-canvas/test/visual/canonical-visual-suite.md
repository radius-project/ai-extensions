# Canonical visual suite inventory

The canonical Canvas visual suite runs `pnpm run test:visual:canonical`, which builds the pinned Docker image and then runs `pnpm run test:visual:stability` inside it.

```console
playwright test --config packages/adapter-canvas/playwright.visual.config.ts --repeat-each=2 --retries=0
```

The suite has 24 unique visual tests. Canonical check mode repeats each test twice with Playwright retries disabled, so a full run executes 48 tests. The tests do not type into text fields; they use seeded fixture state, mocked API routes, button clicks, select/dropdown choices, synthetic change events, and full-page screenshot comparisons.

## Shared fixture state

Each case starts from the real Canvas harness with fixture repository `fixture/radius-app`, workspace branch `feature/phase-6`, fixture `.radius/app.bicep`, fixture `src/web/app.ts`, deterministic fake CLI responses, locally injected Inter font, deterministic light or dark theme variables, a 1440 by 1000 viewport, reduced motion, hidden carets, and disabled animation and transition timing.

## Tests

| Test                                                  | Page and state                                                                                                                                 | UI actions                                                                                                                                                                                                                                                      | Key assertions before screenshot                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Screenshot baseline                                                              |
|-------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------|
| `VI-01 modeled graph details closed in light`         | `?page=graph`, light theme, seeded modeled graph                                                                                               | None                                                                                                                                                                                                                                                            | 4 graph nodes render; `#graph-app` is `radius-app`; `#graph-branch` is `feature/phase-6`; intercepted `/api/load-graph` requests target `fixture/radius-app` and `feature/phase-6`; `#node-popup` is hidden.                                                                                                                                                                                                                                                                                           | `vi-01-modeled-graph-light.png`                                                  |
| `VI-01 modeled graph details closed in dark`          | `?page=graph`, dark theme, seeded modeled graph                                                                                                | None                                                                                                                                                                                                                                                            | 4 graph nodes render; `#graph-app` is `radius-app`; `#graph-branch` is `feature/phase-6`; intercepted `/api/load-graph` requests target `fixture/radius-app` and `feature/phase-6`; `#node-popup` is hidden.                                                                                                                                                                                                                                                                                           | `vi-01-modeled-graph-dark.png`                                                   |
| `VI-02 modeled graph details in light`                | `?page=graph`, light theme, seeded modeled graph                                                                                               | Clicks the `Show details` button on the graph node containing `web`.                                                                                                                                                                                            | `#graph-app` is `radius-app`; `#graph-branch` is `feature/phase-6`; intercepted `/api/load-graph` requests target `fixture/radius-app` and `feature/phase-6`; `#node-popup` is visible.                                                                                                                                                                                                                                                                                                                | `vi-02-modeled-graph-details-light.png`                                          |
| `VI-02 modeled graph details in dark`                 | `?page=graph`, dark theme, seeded modeled graph                                                                                                | Clicks the `Show details` button on the graph node containing `web`.                                                                                                                                                                                            | `#graph-app` is `radius-app`; `#graph-branch` is `feature/phase-6`; intercepted `/api/load-graph` requests target `fixture/radius-app` and `feature/phase-6`; `#node-popup` is visible.                                                                                                                                                                                                                                                                                                                | `vi-02-modeled-graph-details-dark.png`                                           |
| `VI-03 planned graph unresolved recipe pack in light` | `?page=planned`, light theme, seeded planned resources where `web` succeeds and `cache` fails with an unresolved recipe-pack message           | Dispatches a `change` event on `#planned-branch`; reloads the planned page; clicks the `Show details` button on the graph node containing `cache`.                                                                                                              | 2 graph nodes render; `#planned-app` is `radius-app`; `#planned-branch` is `feature/phase-6`; `#plan-btn` is enabled before the branch-change event and disabled after it; intercepted `/api/plan-graph` requests target `fixture/radius-app` and `feature/phase-6`; the popup contains `No recipe pack registered in this environment resolves Radius.Data/redisCaches.`                                                                                                                              | `vi-03-planned-unresolved-light.png`                                             |
| `VI-03 planned graph unresolved recipe pack in dark`  | `?page=planned`, dark theme, seeded planned resources where `web` succeeds and `cache` fails with an unresolved recipe-pack message            | Dispatches a `change` event on `#planned-branch`; reloads the planned page; clicks the `Show details` button on the graph node containing `cache`.                                                                                                              | 2 graph nodes render; `#planned-app` is `radius-app`; `#planned-branch` is `feature/phase-6`; `#plan-btn` is enabled before the branch-change event and disabled after it; intercepted `/api/plan-graph` requests target `fixture/radius-app` and `feature/phase-6`; the popup contains `No recipe pack registered in this environment resolves Radius.Data/redisCaches.`                                                                                                                              | `vi-03-planned-unresolved-dark.png`                                              |
| `VI-04 graph diff with all statuses in light`         | `?page=graph-diff`, light theme, seeded diff from base `main` to head `feature/phase-6` with added, modified, unchanged, and removed resources | None                                                                                                                                                                                                                                                            | 5 graph nodes render; 3 graph edges render; `#base-branch` is `main`; `#head-branch` is `feature/phase-6`; `+1 added` is visible.                                                                                                                                                                                                                                                                                                                                                                      | `vi-04-graph-diff-all-statuses-light.png`                                        |
| `VI-04 graph diff with all statuses in dark`          | `?page=graph-diff`, dark theme, seeded diff from base `main` to head `feature/phase-6` with added, modified, unchanged, and removed resources  | None                                                                                                                                                                                                                                                            | 5 graph nodes render; 3 graph edges render; `#base-branch` is `main`; `#head-branch` is `feature/phase-6`; `+1 added` is visible.                                                                                                                                                                                                                                                                                                                                                                      | `vi-04-graph-diff-all-statuses-dark.png`                                         |
| `VI-05 credential profile list in light`              | `?page=credentials`, light theme, credentials subtab active                                                                                    | None                                                                                                                                                                                                                                                            | Credential profile `fixture-azure` is visible.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `vi-05-credential-profile-list-light.png`                                        |
| `VI-05 credential profile list in dark`               | `?page=credentials`, dark theme, credentials subtab active                                                                                     | None                                                                                                                                                                                                                                                            | Credential profile `fixture-azure` is visible.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `vi-05-credential-profile-list-dark.png`                                         |
| `VI-05 credential profile form in light`              | `?page=credentials`, light theme, credentials subtab active                                                                                    | Clicks `New Credential Profile`.                                                                                                                                                                                                                                | `#cred-form` is visible; `#cred-ghcr-status` contains `using the stored GitHub CLI credential`.                                                                                                                                                                                                                                                                                                                                                                                                        | `vi-05-credential-profile-form-light.png`                                        |
| `VI-05 credential profile form in dark`               | `?page=credentials`, dark theme, credentials subtab active                                                                                     | Clicks `New Credential Profile`.                                                                                                                                                                                                                                | `#cred-form` is visible; `#cred-ghcr-status` contains `using the stored GitHub CLI credential`.                                                                                                                                                                                                                                                                                                                                                                                                        | `vi-05-credential-profile-form-dark.png`                                         |
| `VI-06 environment list in light`                     | `?page=environment`, light theme, environments subtab active                                                                                   | None                                                                                                                                                                                                                                                            | Environment `fixture-environment` is visible.                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `vi-06-environment-list-light.png`                                               |
| `VI-06 environment list in dark`                      | `?page=environment`, dark theme, environments subtab active                                                                                    | None                                                                                                                                                                                                                                                            | Environment `fixture-environment` is visible.                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `vi-06-environment-list-dark.png`                                                |
| `VI-06 environment create form in light`              | `?page=environment`, light theme, environments subtab active                                                                                   | Clicks `New Environment`; clicks `#env-profile-button`; selects profile option matching `fixture-azure`; clicks `#env-step1-next`; selects Azure resource group `fixture-resource-group`; selects Azure cluster `fixture-aks`; scrolls `#deploy-btn` into view. | Details step is visible; GitHub account button is named `@repo-user`; identity note is `Ready to configure deployments`; technical details contain `credential source: keyring`; `Re-check` is enabled; `#deploy-btn` is enabled; discovery status is `Found 1 cluster(s), 1 resource group(s)`; resource group, cluster, and namespace selects resolve to `fixture-resource-group`, `fixture-aks`, and `default`; identity and infrastructure sections are visible; deploy button is in the viewport. | `vi-06-environment-create-light.png`, `vi-06-environment-create-lower-light.png` |
| `VI-06 environment create form in dark`               | `?page=environment`, dark theme, environments subtab active                                                                                    | Clicks `New Environment`; clicks `#env-profile-button`; selects profile option matching `fixture-azure`; clicks `#env-step1-next`; selects Azure resource group `fixture-resource-group`; selects Azure cluster `fixture-aks`; scrolls `#deploy-btn` into view. | Details step is visible; GitHub account button is named `@repo-user`; identity note is `Ready to configure deployments`; technical details contain `credential source: keyring`; `Re-check` is enabled; `#deploy-btn` is enabled; discovery status is `Found 1 cluster(s), 1 resource group(s)`; resource group, cluster, and namespace selects resolve to `fixture-resource-group`, `fixture-aks`, and `default`; identity and infrastructure sections are visible; deploy button is in the viewport. | `vi-06-environment-create-dark.png`, `vi-06-environment-create-lower-dark.png`   |
| `VI-07 deploy success in light`                       | `?page=deploying`, light theme, mocked deployment list with status `success`                                                                   | None                                                                                                                                                                                                                                                            | Deployment table body shows exact text `Success`.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `vi-07-deploy-success-light.png`                                                 |
| `VI-07 deploy failed in light`                        | `?page=deploying`, light theme, mocked deployment list with status `failed`                                                                    | None                                                                                                                                                                                                                                                            | Deployment table body shows exact text `Failed`.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `vi-07-deploy-failed-light.png`                                                  |
| `VI-07 deploy success in dark`                        | `?page=deploying`, dark theme, mocked deployment list with status `success`                                                                    | None                                                                                                                                                                                                                                                            | Deployment table body shows exact text `Success`.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `vi-07-deploy-success-dark.png`                                                  |
| `VI-07 deploy failed in dark`                         | `?page=deploying`, dark theme, mocked deployment list with status `failed`                                                                     | None                                                                                                                                                                                                                                                            | Deployment table body shows exact text `Failed`.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `vi-07-deploy-failed-dark.png`                                                   |
| `VI-08 run-command callout in light`                  | `?page=credentials`, light theme, credentials subtab active, GitHub keyring scopes reduced to `repo`                                           | Clicks `New Credential Profile`; scrolls `#cred-ghcr-command-row` into view.                                                                                                                                                                                    | `#cred-form` is visible; command row is visible; command row contains `gh auth refresh`; command row has `Run with Copilot` and `Copy` buttons; `#cred-ghcr-retry` is visible.                                                                                                                                                                                                                                                                                                                         | `vi-08-run-command-callout-light.png`                                            |
| `VI-08 run-command callout in dark`                   | `?page=credentials`, dark theme, credentials subtab active, GitHub keyring scopes reduced to `repo`                                            | Clicks `New Credential Profile`; scrolls `#cred-ghcr-command-row` into view.                                                                                                                                                                                    | `#cred-form` is visible; command row is visible; command row contains `gh auth refresh`; command row has `Run with Copilot` and `Copy` buttons; `#cred-ghcr-retry` is visible.                                                                                                                                                                                                                                                                                                                         | `vi-08-run-command-callout-dark.png`                                             |
| `VI-09 wizard github access callout in light`         | `?page=environment`, light theme, environments subtab active, GitHub keyring scopes reduced to `repo`                                          | Clicks `New Environment`; clicks `#env-profile-button`; selects profile option matching `fixture-azure`; clicks `#env-step1-next`; scrolls `#env-gh-repair` into view.                                                                                          | Details step is visible; repair callout is visible; repair callout contains `gh auth switch`; repair callout does not contain `In the terminal, run`; repair callout has `Run with Copilot` and `Copy` buttons.                                                                                                                                                                                                                                                                                        | `vi-09-wizard-github-callout-light.png`                                          |
| `VI-09 wizard github access callout in dark`          | `?page=environment`, dark theme, environments subtab active, GitHub keyring scopes reduced to `repo`                                           | Clicks `New Environment`; clicks `#env-profile-button`; selects profile option matching `fixture-azure`; clicks `#env-step1-next`; scrolls `#env-gh-repair` into view.                                                                                          | Details step is visible; repair callout is visible; repair callout contains `gh auth switch`; repair callout does not contain `In the terminal, run`; repair callout has `Run with Copilot` and `Copy` buttons.                                                                                                                                                                                                                                                                                        | `vi-09-wizard-github-callout-dark.png`                                           |
