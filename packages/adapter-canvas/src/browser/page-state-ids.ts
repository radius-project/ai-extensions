// Page state element ids are shared between a page and the shell behavior that
// has to detect it, so they live outside the page modules to keep the shell
// from importing a page.
export const GRAPH_PAGE_STATE_ID = "radius-graph-page-state";
