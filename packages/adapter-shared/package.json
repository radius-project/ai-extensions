{
  "name": "@radius-project/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Node-runtime helpers shared across Radius adapters (e.g. building application graphs via the real rad CLI). Depends on @radius-project/core; contains the impure I/O that core intentionally excludes.",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "eslint \"src/**/*.ts\" vitest.config.ts"
  },
  "dependencies": {
    "@radius-project/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
